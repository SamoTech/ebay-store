# Legacy peer dependencies for testing library compatibility
legacy-peer-deps=true

# Vercel-specific settings
prefer-frozen-lockfile=false
engine-strict=false

# Speed optimizations
prefer-offline=true
progress=false
